# 新手引导 - 第二阶段功能导览方案

## 触发时机

第一阶段（螂王对话）全部完成后自动进入第二阶段。

## 流程概述

```
第一阶段完成
    ↓
第二阶段开始 → 主页气泡显示指引文字 + 小箭头指向菜单
    ↓
用户点击菜单展开按钮（home-toggle-hotspot）
    ↓
菜单展开，显示 5 个功能按钮（从上到下）
    ↓
箭头指向第一个功能按钮（AI任务拆解）
    ↓
用户点击 → 跳转对应页面 → 气泡显示简短功能说明
    ↓
用户点击返回 → 回到主页 → 箭头指向下一个按钮
    ↓
（重复 5 次，依次：AI → 抽卡 → 待办 → 回顾 → 收藏）
    ↓
全部完成后 → 进入第三阶段
```

## 精简短文案（每条 15 字以内）

| 功能 | 主页指引 | 页面内说明 |
|------|---------|-----------|
| AI任务拆解 | 「点这个，拆任务」 | 「写任务→读书螂帮你拆成小步骤」 |
| 任务选择 | 「点这个，抽卡牌」 | 「选一张卡牌，开启冒险」 |
| 待办管理 | 「点这个，看清单」 | 「勾掉完成的任务，超有成就感」 |
| 任务回顾 | 「点这个，看记录」 | 「走过的每一步都记在这里」 |
| 卡牌收藏 | 「点这个，看图鉴」 | 「收集角色卡牌，解锁故事」 |

## 视觉规范

- 主页指引：大气泡（左侧）+ 小三角箭头（指向按钮右侧）
- 功能页面：大气泡（居中）显示简短说明
- 所有指引气泡半透明背景，不遮挡页面主要内容

## 交互细节

1. 菜单按钮（home-toggle-hotspot）默认不可见，第二阶段开始后高亮闪烁
2. 用户点击展开菜单 → 显示 5 个功能按钮
3. 箭头指向当前目标按钮，其他按钮半透明
4. 用户点击目标按钮 → 正常跳转页面
5. 页面加载后气泡显示说明文字
6. 用户点击返回 → 回到主页 → 箭头移到下一个按钮
7. 五个全部点完 → 自动进入第三阶段

## 约束

- 不使用引导覆盖层遮罩，第二阶段主页应可正常交互
- 仅用气泡 + 箭头做指引，不强干扰用户操作
- 用户可自行决定点击节奏，不强制限时
- 所有文案口语化、简短、贴合螂王世界观
